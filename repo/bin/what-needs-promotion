#!/bin/bash

BINDIR=$(dirname $0)
DISTS=$BINDIR/../dists
DIST=${1:-sid}

$BINDIR/diff-dists \
	<(bzcat $DISTS/$DIST-pgdg-testing/main/source/Sources.bz2) \
	<(bzcat $DISTS/$DIST-pgdg/main/source/Sources.bz2) |
grep -v -e '^bordeaux-threads' -e '^buildapp' -e '^cffi' -e '^cl'
